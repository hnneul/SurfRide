// 논리 해상도
export const LOGICAL_WIDTH  = 1920;
export const LOGICAL_HEIGHT = 1080;

// 게임 상태
export const GameState = Object.freeze({
  MAIN:     'MAIN',
  WORLDMAP: 'WORLDMAP',
  PLAYING:  'PLAYING',
  PAUSED:   'PAUSED',
  RESULT:   'RESULT',
});
